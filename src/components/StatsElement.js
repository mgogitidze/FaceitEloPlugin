function StatsElement(props) {
    return <div className="ds">
        <div className="ds_element_first">{props.value}</div>
        <div className="ds_element_first">{props.name}</div>
    </div>
}

export default StatsElement